def perimeter(length, width):
    h = length * width
    l = length
    return print((2 * l) + (2 * h))

def rectanglearea(length, width):
    h = length * width
    l = length
    return print(h * l)

